defaults:
  - /dataset: p3
  - /encoder: pointpillars_vit_dinov3
  - /model: pix2poly
  - _self_

name: lidar_dinov3_shuffle_polys
group_name: v3_${.model.name}
project_name: HiSup

lidar_dropout: null

dataset:
  country: CH
  annotation_type: blocks

model:
  shuffle_polygons: true