name: inria
path: /data/rsulzer/${model.name}_data/${.name}
train:
  annotations: ${..path}/train/annotation.json
  images: ${..path}/train/images/
val:
  annotations: ${..path}/val/annotation.json
  images: ${..path}/val/images/
test:
  annotations: ${..path}/test/annotation.json  
  images: ${..path}/test/images/
